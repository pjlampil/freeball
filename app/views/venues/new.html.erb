<% content_for :title, "New Venue" %>
<div class="space-y-5">
  <div class="flex items-center gap-3">
    <%= link_to "←", venues_path, class: "text-gray-400 hover:text-white text-xl" %>
    <h1 class="text-2xl font-bold">New Venue</h1>
  </div>
  <%= render "form", venue: @venue %>
</div>
